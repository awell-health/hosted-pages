# import 'SessionActivity'
subscription OnSessionActivityCreated($only_stakeholder_activities: Boolean!) {
  sessionActivityCreated(only_stakeholder_activities: $only_stakeholder_activities) {
    ...SessionActivity
  }
}