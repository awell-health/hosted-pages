# import 'HostedSession'
query GetHostedSession {
  hostedSession {
    session {
      ...HostedSession
    }
    branding {
      logo_url
      hosted_page_title
      accent_color
      hosted_page_auto_progress
    }
  }
}
